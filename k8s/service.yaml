apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: social-network-dev
spec:
  selector:
    app: frontend-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP