apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-app
  namespace: social-network-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      containers:
      - name: frontend
        image: socialworky/worky-frontend-dev:2fd0998e461f6d4af184f683de8f9e6c33fa9e4f
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
